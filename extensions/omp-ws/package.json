{
  "name": "@wsagency/omp-ws",
  "version": "0.4.0",
  "description": "WS Agency full-native omp suite: all WS commands/skills/agents/rules (generated from the marketplace source) plus dangerous-git guard, changelog gate, Jira session dashboard, docs-drift stop nudge, OpenWiki freshness, compaction preservation, and ws_ticket/ws_changelog/ws_adr tools.",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "commands",
    "skills",
    "agents",
    "rules",
    "README.md"
  ],
  "omp": {
    "extensions": [
      "./dist/index.js"
    ],
    "settings": {
      "jiraProject": {
        "type": "string",
        "description": "Jira project key the session dashboard scopes to (overrides .claude/ws-project.yaml binding)",
        "env": "JIRA_PROJECT",
        "default": ""
      },
      "guard": {
        "type": "boolean",
        "description": "Enable the fail-safe dangerous-git/rm bash guard",
        "default": true
      },
      "dashboard": {
        "type": "boolean",
        "description": "Enable the Jira workload widget on session start",
        "default": true
      }
    }
  },
  "scripts": {
    "generate": "bun run scripts/generate.ts",
    "build": "bun run generate && bun build ./src/index.ts --outdir ./dist --target=bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.1.5",
    "@types/bun": "^1.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "license": "UNLICENSED",
  "author": "WS Agency"
}
