{
  "name": "@wsagency/omp-ws",
  "version": "0.7.0",
  "description": "WS Agency full-native omp suite: seven commands, 30 generated skills, 14 agents, and four rules from the marketplace source, plus canonical .wsagency/config.yaml runtime policy, dangerous-git guard, changelog gate, Jira session dashboard, docs-drift stop nudge, OpenWiki freshness, compaction preservation, and ws_ticket/ws_changelog/ws_adr tools.",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "commands",
    "skills",
    "agents",
    "rules",
    "templates",
    "scripts/outline-sync.py",
    "scripts/parse-git-log.sh",
    "scripts/validate-changelog.sh",
    "README.md"
  ],
  "omp": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "generate": "bun run scripts/generate.ts",
    "build": "bun run generate && bun build ./src/index.ts --outdir ./dist --target=bun",
    "prepack": "bun run build",
    "test": "bun test ./test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.2.4",
    "@types/bun": "^1.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "license": "UNLICENSED",
  "author": "WS Agency"
}
