# Domain Context Map

This repository uses the multi-context domain layout configured in `.wsagency/config.yaml`. Add each bounded context and its context file here when it becomes known; system-wide architectural decisions live in `dev-docs/decisions/`.

## Contexts

No bounded contexts have been recorded yet.
