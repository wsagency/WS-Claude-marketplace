# __PROJECT_NAME__ — Project Hub

> Single source of truth for the __PROJECT_NAME__ project. Sub-repos live as subfolders here, each with its own git and its own `CLAUDE.md`.

## What this is

__PROJECT_DESCRIPTION__

Each sub-repo is a self-contained git repo in a subfolder. The hub itself is a small git repo that tracks only this `CLAUDE.md`, `project.yaml`, `invoke-ai.sh`, `README.md`, and `.gitignore`. The sub-repo directories are listed in the managed block of `.gitignore` so they don't pollute the hub's git history.

## How Claude loads context

`./invoke-ai.sh` launches Claude with `--add-dir` for every sub-repo present locally. Claude Code automatically reads:

- This file (`<hub>/CLAUDE.md`) — high-level project map
- Each `<sub-repo>/CLAUDE.md` — rules specific to that codebase

Per-repo rules live in the repo they apply to. Edit `acme-app/CLAUDE.md` for app-specific conventions, etc.

## Sub-repos

<!-- ws-hub:repos:start -->
<!-- generated from project.yaml — edit `description` / `tech` fields there, then re-run /ws-hub-describe to refresh this section -->

__REPO_SECTIONS__

<!-- ws-hub:repos:end -->

## Cross-repo notes

<!-- Hand-written. Document how the repos interact: shared contracts, deploy order, design-token flow, API boundaries. -->

- _Add cross-repo dependencies, shared contracts, and workflow notes here._

## Working with this hub

- `./invoke-ai.sh` — launch Claude with all accessible sub-repos mounted
- `/ws-hub-repos clone` — clone every registered repo URL into a missing subfolder
- `/ws-hub-repos pull` — `git pull` across all sub-repos
- `/ws-hub-status` — aggregated git status across all sub-repos
- `/ws-hub-add-repo --scan` — detect un-registered sub-repos
- `/ws-hub-add-repo` — register a new sub-repo (clone or adopt)
- `/ws-hub-describe` — refresh sub-repo descriptions from their READMEs

## Access notes

Sub-repos you don't have git access to will simply be missing from disk and `invoke-ai.sh` will skip them with a warning. The hub itself contains no sensitive code — it's a meta repo, safe to share broadly.

Hub conventions are documented in `.claude/skills/project-hub-conventions/SKILL.md` (vendored locally — works without the ws-marketplace plugin installed).
