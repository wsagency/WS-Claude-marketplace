# Prove installed Claude and omp parity

**What to build:** Prove the breaking setup cutover from the actual installed marketplace checkout and the exact native tarball intended for publication, using one deterministic scenario matrix and permanent positive and absence gates.

**Blocked by:** 31-remove-legacy-setup-surface-and-readers

**Status:** done

- [x] The marketplace and native package metadata declare the approved 5.0.0 and 0.7.0 release pair before the release artifacts are built.
- [x] The full deterministic manifest-transaction suite covers first run, every versioned legacy fixture, aligned no-op, conflict zero-write, all tracker modes, docs failure, hub failure, drift, every reconfiguration domain, interruption, resume, and valid partial acceptance.
- [x] A clean isolated marketplace installation discovers the public command and both workers, can reach the packaged schema and transaction support, completes a representative migration and aligned rerun, and exposes no removed surface.
- [x] An actual native package archive is installed into a clean isolated omp profile and proves the same discovery, representative migration, no-op, runtime behavior, and absence contract.
- [x] Source-tree success alone cannot satisfy the gate; the tested archive is retained with identity evidence so the release ticket publishes that exact artifact.
- [x] Jira behavior uses deterministic fake adapters; automated and release verification perform no live Jira mutations.
- [x] Package contents, generated surface counts, references, migration guide, changelog mirror, version pair, and clean-install metadata all pass release assertions.
- [x] Any failed scenario, missing artifact, unexpected legacy entry, package drift, or isolated-install failure blocks publication.
